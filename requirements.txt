# ============================================================
# BinaryClash — Зависимости Python
# Версия: 1.0 (MVP)
# ============================================================

# --- Core Web Framework ---
Flask==2.3.3
Werkzeug==2.3.7
Jinja2==3.1.2

# --- Database ---
SQLAlchemy==2.0.20

# --- Authentication & Security ---
Authlib==1.3.0          # Для Google OAuth
python-dotenv==1.0.0    # Для загрузки .env файлов

# --- HTTP Requests (для работы с ИИ-API) ---
requests==2.31.0

# --- Image Processing (для аватаров) ---
Pillow==10.0.0

# ============================================================
# Встроенные модули Python (НЕ ТРЕБУЮТ УСТАНОВКИ):
# - os, json, random, secrets, smtplib, io, base64, email, functools, sqlalchemy.orm
# ============================================================